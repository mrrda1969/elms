import { Grid, Typography } from "@mui/material";

const Student = () => {
  return (
    <Grid container>
      <Grid item>
        <Typography>Student Page</Typography>
      </Grid>
    </Grid>
  );
};

export default Student;
